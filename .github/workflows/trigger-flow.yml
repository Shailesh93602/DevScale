name: ContextQA Testing Workflow

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  contextqa-testing:
    uses: ./contextqa-workflow.yml
    with:
      cqa_api_url: ${{ secrets.CQA_API_URL }}
      cqa_api_secret: ${{ secrets.CQA_API_SECRET }}
      cqa_project_title: ${{ secrets.CQA_PROJECT_TITLE }}
      cqa_deployment_url: ${{ secrets.CQA_DEPLOYMENT_URL }}
