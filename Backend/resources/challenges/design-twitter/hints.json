["Use a Map to track followers. `followers.get(userId)` should return a Set of userIds that `userId` follows.", "Use a global counter or timestamp to track the order of tweets.", "You can use a Max-Heap to efficiently retrieve the 10 most recent tweets from all followed users. Alternatively, if tweets are stored sequentially, you can iterate backwards through all tweets until you find 10 from followed users."]