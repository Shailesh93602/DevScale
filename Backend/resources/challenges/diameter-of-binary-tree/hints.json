["The diameter through any node is left subtree height + right subtree height.", "You need to compute the HEIGHT of each subtree. At every node, update a global maximum with left_height + right_height.", "Use a post-order DFS — calculate children heights before the current node.", "The helper function returns HEIGHT (number of edges to deepest leaf), while the main function tracks the global maximum DIAMETER."]