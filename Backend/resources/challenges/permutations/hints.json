["To generate permutations, you need to iterate through the array and select each unused element to be the next part of your permutation.", "Use a boolean array, or a Set, or just search `current.includes(num)` to keep track of which elements you have already used in your current recursive branch.", "Once your path length equals the original array length, you've found a valid permutation. Don't forget to push a copy of it!"]