[
  "The goal is to render data as text, not as HTML. You need to replace characters that have special meaning in HTML.",
  "Consider using a Regular Expression to find all occurrences of `<` and other characters.",
  "Create a lookup map where keys are characters and values are their corresponding HTML entities (e.g., `&lt;` for `<`)."
]