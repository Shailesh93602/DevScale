[
  "Standard SQL window functions like RANK(), DENSE_RANK(), and ROW_NUMBER() are useful here.",
  "What is the difference between RANK() and DENSE_RANK()? Which one ensures no gaps in the ranking values?",
  "Ensure you use 'rank' as an alias, but be careful as 'rank' is often a reserved keyword in some SQL dialects (wrap it in quotes if necessary)."
]