[
  "Root nodes are easy to identify: they have no parent (p_id IS NULL).",
  "Inner nodes are nodes that have a parent (p_id IS NOT NULL) AND act as a parent to at least one other node.",
  "Leaf nodes are nodes that have a parent but do not appear in the p_id column of any row."
]