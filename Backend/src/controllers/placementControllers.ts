import { catchAsync } from '../utils/index';

export const getResources = catchAsync(async () => {
  // TODO: add logic for the controller or remove the controller entirely
});

export const getBooks = catchAsync(async () => {
  // TODO: add logic for the controller or remove the controller entirely
});
